resources:
  - configmap.yaml
  - genesis.yaml
  - validator.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: test-addresses-gaiad
    files:
      - configs/keys.json
      - configs/validators.json
