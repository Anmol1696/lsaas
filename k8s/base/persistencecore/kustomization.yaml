resources:
  - configmap.yaml
  - genesis.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: test-addresses-persistencecore
    files:
      - configs/keys.json
      - configs/validators.json
