resources:
  - configmap.yaml
  - genesis.yaml
  - validator.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: test-addresses-osmosisd
    files:
      - configs/keys.json
      - configs/validators.json
