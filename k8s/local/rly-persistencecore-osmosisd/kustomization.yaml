namespace: default

resources:
  - ../../base/rly-persistencecore-osmosisd

patchesStrategicMerge:
  - relayer.yaml
  - ts-relayer.yaml
  ## Commenting out validator since for local setup no need for validator nodes
  #- validator.yaml
